springdoc:
  swagger-ui.path: /openapi/swagger-ui.html
  api-docs.path: /openapi/v3/api-docs
  packagesToScan: dev.kalbarczyk.exerciseservice.services
  pathsToMatch: /**
  cache.disabled: true

  swagger-ui:
    oauth2-redirect-url: https://localhost:8443/exercises/openapi/swagger-ui/oauth2-redirect.html
    oauth:
      clientId: writer
      clientSecret: secret-writer
      useBasicAuthenticationWithAccessCodeGrant: true
  oAuthFlow:
    authorizationUrl: https://localhost:8443/oauth2/authorize
    tokenUrl: https://localhost:8443/oauth2/token

server.forward-headers-strategy: framework

api:
  common:
    version: 1.0.0
    title: Exercise Service API
    description: API for the Exercise Service
    termsOfService: example.com/terms
    license: Example License
    licenseUrl: example.com/license
    externalDocDesc: Example External Docs
    externalDocUrl: example.com/repository
    contact:
      name: Oskar Kalbarczyk
      url: https://kalbarczyk.dev
      email: example@email.com

  responseCodes:
    ok.description: OK
    badRequest.description: Bad Request, invalid format of the request. See response message for more information
    notFound.description: Not found, the specified id does not exist
    unprocessableEntity.description: Unprocessable entity, input parameters caused the processing to fail. See response message for more information

  exercise:
    getExercises:
      description: Gets a list of exercises
      notes: |
        # Normal response
        A list of all exercises will be returned. If no exercises are found an empty list will be returned.

    getExercise:
      description: Gets the exercise information.
      notes: |
        # Normal response
        If the requested exerciseId is found the method will return information regarding:
        1. Exercise

        # Expected partial and error responses
        1. If no exercise information is found, a **404 - Not Found** error will be returned

    createExercise:
      description: Creates a new exercise
      notes: |
        # Normal response
        The createExercise information posted to the API will be stored as a new exercise entity.

        # Expected error responses
        1. If an exercise with the same name as specified in the posted information already exists,
           an **422 - Unprocessable Entity** error with a "duplicate key" error message will be returned

    deleteExercise:
      description: Deletes an exercise
      notes: |
        # Normal response
        The exercise related to the specified exerciseId will be deleted.

server.port: 8080
server.error.include-message: always



spring.security.oauth2.resourceserver.jwt.issuer-uri: http://${app.auth-server}:9999



spring.data.mongodb:
  host: mongodb
  port: 27017
  database: mongo-db
  username: root
  password: pwd
  authentication-database: admin



logging:
  level:
    root: INFO
    dev.kalbarczyk: INFO
    org.springframework.data.mongodb.core.ReactiveMongoTemplate: DEBUG

